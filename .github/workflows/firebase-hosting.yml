# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
name: Deploy Preview

on: [pull_request]

jobs:
  deploy_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # If your website has any build steps, run them here. For example:
      # - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0.1-alpha
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount:
            "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ACTION_HOSTING_DEPLOY_DEMO }}"
          projectId: action-hosting-deploy-demo
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
